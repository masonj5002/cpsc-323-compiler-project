@
    /* Empty Function Declaration List */
@
    integer x, y, z; /* Variables for our experiment */
@
    read(x, y, z);
    write(x);
    write(y);
    write(z);
    
    if (x > y)
        z = 100;
    otherwise  
        z = -1;
    fi
@
